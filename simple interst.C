#include <stdio.h>

int main(void) {
  printf("Enter the value of principal,rate and time\n");
  float p,r,t,si;
  scanf("%f%f%f",&p,&#ir,&t);
  si=(p*r*t)/100;
  printf("The simple interest is %f",si);
  return 0;
}
